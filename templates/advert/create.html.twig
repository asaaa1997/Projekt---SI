{% include 'default/_form.html.twig' with {
    page_title: 'title_advert_create',
    attr: {
        method: 'POST',
        action: url('advert_create')
    },
    form: form,
    submit_label: 'action_create'|trans,
    back_to_list_path: 'advert_index',
} %}
