{% include 'default/_form.html.twig' with {
    page_title: 'title_advert_edit',
    page_title_param: {'%id%': advert.id},
    attr: {
        method: 'PUT',
        action: url('advert_edit', {'id': advert.id}),
    },
    form: form,
    back_to_list_path: 'advert_index',
} %}